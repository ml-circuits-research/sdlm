@input tokens
@input predicate
@input qualifier
@input subjectIndex
@input objectIndex
@subject termAt
    tokens $tokens
    index $subjectIndex
@object termAt
    tokens $tokens
    index $objectIndex
@atom makeBinaryAtom
    subject $subject
    predicate $predicate
    qualifier $qualifier
    object $object
    polarity "positive"
@command makeCommand
    kind "assertFact"
    payload $atom
@output result $command
