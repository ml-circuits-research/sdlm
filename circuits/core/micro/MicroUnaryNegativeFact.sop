@input tokens
@input predicate
@subject termAt
    tokens $tokens
    index 0
@atom makeUnaryAtom
    subject $subject
    predicate $predicate
    polarity "negative"
@command makeCommand
    kind "assertFact"
    payload $atom
@output result $command
